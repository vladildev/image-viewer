<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ToolBar?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.VBox?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            prefHeight="600.0" prefWidth="800.0">
    
    <!-- Barre d'outils à gauche -->
    <left>
        <VBox spacing="10.0" style="-fx-background-color: #f4f4f4; -fx-padding: 10;">
            <padding>
                <Insets top="10.0" right="10.0" bottom="10.0" left="10.0"/>
            </padding>
            
            <Button fx:id="zoomButton" text="🔍" prefWidth="120" 
                    onAction="#handleZoom"/>
            
            <Button fx:id="translateButton" text="↔️" prefWidth="120" 
                    onAction="#handleTranslate"/>
            
            <Button fx:id="importImageButton" text="📁" prefWidth="120" 
                    onAction="#handleImportImage"/>
            
            <Button fx:id="saveImageButton" text="💾" prefWidth="120" 
                    onAction="#handleSaveImage"/>
            
            <Button fx:id="importConfigButton" text="⚙️" prefWidth="120" 
                    onAction="#handleImportConfig"/>
            
            <Button fx:id="saveConfigButton" text="💿" prefWidth="120" 
                    onAction="#handleSaveConfig"/>
            
            <Button fx:id="undoButton" text="↩️" prefWidth="120" 
                    onAction="#handleUndo"/>
            
            <Button fx:id="redoButton" text="↪️" prefWidth="120" 
                    onAction="#handleRedo"/>
        </VBox>
    </left>
    
    <!-- Image centrale -->
    <center>
        <ImageView fx:id="imageView" 
                   preserveRatio="true" 
                   fitWidth="600.0" 
                   fitHeight="500.0"
                   style="-fx-background-color: white;">
            <BorderPane.margin>
                <Insets top="20.0" right="20.0" bottom="20.0" left="20.0"/>
            </BorderPane.margin>
        </ImageView>
    </center>
    
</BorderPane>
